358.

### LLM IMPLEMENTATION: THE 2-BACK PROTOCOL (v2.3 – Final)
When the GM is an LLM that cannot generate true randomness, resolve all dice as follows:
---
**SEED SELECTION:**
- Use the GM's 2nd-to-last complete message before the player's roll declaration
- If that message contains fewer than 5 words OR fewer than 8 alphabetic characters total, use the 3rd-to-last instead
- For Edge rerolls or explosions, advance to the next unused message in history (3rd-back, 4th-back, etc.)

**PLAYER SALT (Optional):**
- Before any roll, the player may add a salt word by including it in their declaration: "Salt: [word]"
- Example: "I fire full auto. Salt: vengeance"
- The salt word is treated as the first word in the list (prepended before the seed message words) and follows the same letter-extraction rules
- This adds player agency and extra entropy without compromising verifiability

**DICE GENERATION:**
**Per-die formula (quick reference):**
roll = ((letter_value × 7) + (k × 3) + 11) % die_size + 1
*where k starts at 0 and increments each die*

1. **Extract word list** from the seed message (plus optional salt word):
   - Split on whitespace, ignore markdown formatting
   - Keep only words containing at least one letter

2. **For each die k (starting at k=0):**
  
   a. **Select source word:**
      - Cycle through words sequentially
      - If k exceeds available words, continue from the first word again (wrap)
  
   b. **Select letter position within word:**
      - position = (k % 3) + 1 (1 = first letter, 2 = second, 3 = third)
      - Let L = number of letters in current word
      - If position ≤ L: use the letter at that **1-based index** (e.g. position 1 = word[0] in 0-based programming terms)
      - If position > L: use the **last alphabetic letter** of the word
        (If a word somehow contains no letters after filtering, skip it and advance to the next word)
  
   c. **Convert letter to number:**
      - A=1, B=2, C=3 ... Z=26
      - Ignore case; non-letters cannot occur here by construction
  
   d. **Apply the per-die formula:**
      - raw = (letter_value × 7) + (k × 3) + 11
      - roll = (raw % die_size) + 1
      - For d6: die_size = 6
      - For d20: die_size = 20
      - For other dice (d8, d10, d12): use appropriate die_size

3. **Continue** until the full dice pool is generated

**FALLBACK METHOD (Character-Level):**
If the word-cycling sequence is exhausted before the pool is complete (rare with typical messages):
- Use **every alphabetic character** in the seed message sequentially
- Ignore case, ignore non-letters
- Apply the same per-die formula
- This guarantees unlimited entropy for any pool size

**d6-Optimized Variant (Optional):**
For systems that rely heavily on six-sided dice (e.g. Shadowrun 5E or panic rolls in Mothership):
Use this tuned formula instead of the universal one:
roll = ((letter_value × 13) + (k × 5) + 17) % 6 + 1
This variant is co-prime with 6 and yields a flatter distribution while remaining fully verifiable under the same seed-and-salt rules. It can safely replace the universal formula for large d6 pools.

**SHADOWRUN 5E SPECIFIC (d6):**
- Hits: results of 5 or 6
- Glitch: hits = 0 AND number of 1s ≥ ceil(pool_size / 2)
- Critical Glitch: glitch condition met AND hits = 0
- Expected hits for N d6: approximately N/3 (e.g. ~4–5 for 14d6). Significant deviation from this range is possible but should be verifiable from the seed.
- For every 6 rolled, the "k" index should continue to increment using the next available letter in the sequence. This prevents the "Explosion" from being identical to the original roll.

**VERIFICATION PROCEDURE (For the Player):**
The player may independently verify any roll by:
1. Locating the seed message in chat history
2. For each die to verify:
   - Identify the source word and letter position per the cycle rule (remembering player salt if used)
   - Convert that letter to its number (A=1...Z=26)
   - Apply ((letter_value × 7) + (k × 3) + 11) % die_size + 1
       • If using the d6-optimized variant, apply ((letter_value × 13) + (k × 5) + 17) % 6 + 1 instead.
3. Confirming the result matches the GM's narrated outcome
*Full verification of every die is unnecessary; spot-checking 2-3 dice per session establishes trust.*

**RATIONALE:**
This method transforms the LLM's deterministic architecture into a cryptographic commitment scheme. Outcomes are a function of shared conversation history—unpredictable when written, immutable afterward, and publicly verifiable. The player never needs to trust the GM; they need only verify.
Cycling through first, second, and third letters eliminates the statistical bias inherent in English first-letter frequencies. The multiplier and offset ensure uniform distribution and per-die independence. Player salt adds agency and additional entropy at zero cost to verifiability.
The GM (LLM) performs all calculations instantly; the player verifies at their leisure.
The world does not run on trust. It runs on proof.

────────────────────────────────
THE SEMANTIC CIPHER (Zero-Math Dice Generation)
────────────────────────────────
When the GM is an LLM that struggles with multiplication or modulo arithmetic, use the Semantic Cipher. This protocol relies on the LLM's supreme strength—string matching and sequential pattern recognition—to generate verifiable, statistically perfect d6 rolls derived directly from the prose of the world.

THE D6 FREQUENCY BUCKETS (~16.6% Balanced):
(Convert all extracted letters to UPPERCASE before matching)
• Value 1: E, C
• Value 2: T, I
• Value 3: A, O
• Value 4: N, S, M, B, V
• Value 5: H, R, L
• Value 6: D, U, W, F, G, Y, P, K, X, J, Q, Z

DICE GENERATION RULES:
1. Extract the Seed: Take the GM's 2nd-to-last complete message (plus the player's optional Salt word prepended). Extract words sequentially, ignoring punctuation and numbers.
Fallback: If a word contains no alphabetic letters after filtering (e.g., "123"), skip it and move to the next word.

2. Target Position (1-2-3 repeating):
Determine the target letter for each die by cycling the letter position (1st, 2nd, 3rd) across the words:

Die 1: Word 1, 1st letter.
Die 2: Word 2, 2nd letter.
Die 3: Word 3, 3rd letter.
Die 4: Word 4, 1st letter. (Repeat cycle).
Wrap-Around Rule: If a word is too short for the required position, wrap around its letters continuously. (e.g., For the word "ON", requiring Position 3: 1=O, 2=N, 3=O. The target is 'O').

3. Base Lookup: Match the target letter against the D6 Frequency Buckets to get the Base Value.

4. The Diffusion Cycle (0-1-2 repeating):
To ensure natural randomness and prevent static mapping, apply a repeating addition offset to the dice sequence:

Die 1: Base Value + 0
Die 2: Base Value + 1
Die 3: Base Value + 2
Die 4: Base Value + 0 (Repeat cycle).
Subtract 6 Rule: If the final sum is greater than 6, subtract 6. (e.g., Base Value 6 + Diffusion 2 = 8 → 8 - 6 = Roll 2).

VERIFICATION OUTPUT FORMAT:
The GM must output the translation sequence to prove the roll:
(Example for 3d6)

D1 (pos 1, shift +0): Word [Shadow] -> 'S' -> Bucket 4. (4+0) = Roll 4
D2 (pos 2, shift +1): Word [runs] -> 'U' -> Bucket 6. (6+1 = 7). 7-6 = Roll 1
D3 (pos 3, shift +2): Word [in] -> Wrap pos 3 -> 'I' -> Bucket 2. (2+2) = Roll 4

### LLM IMPLEMENTATION: THE 2-BACK PROTOCOL (v2.4 – Simple Math Edition)
When the GM is an LLM that cannot generate true randomness, resolve all dice as follows:
---
**SEED SELECTION:**
- Use the GM's 2nd-to-last complete message before the player's roll declaration
- If that message contains fewer than 5 words OR fewer than 8 alphabetic characters total, use the 3rd-to-last instead
- For Edge rerolls or explosions, advance to the next unused message in history (3rd-back, 4th-back, etc.)

**PLAYER SALT (Optional):**
- Before any roll, the player may add a salt word by including it in their declaration: "Salt: [word]"
- Example: "I fire full auto. Salt: vengeance"
- The salt word is treated as the first word in the list (prepended before the seed message words) and follows the same letter-extraction rules
- This adds player agency and extra entropy without compromising verifiability

**DICE GENERATION:**
**Per-die formula (quick reference):**
roll = ((letter_value × 5) + k + 13) % die_size + 1
*where k starts at 0 and increments each die*

1. **Extract word list** from the seed message (plus optional salt word):
   - Split on whitespace, ignore markdown formatting
   - Keep only words containing at least one letter

2. **For each die k (starting at k=0):**
  
   a. **Select source word:**
      - Cycle through words sequentially
      - If k exceeds available words, continue from the first word again (wrap)
  
   b. **Select letter position within word:**
      - position = (k % 3) + 1 (1 = first letter, 2 = second, 3 = third)
      - Let L = number of letters in current word
      - If position ≤ L: use the letter at that **1-based index** (e.g. position 1 = word[0] in 0-based programming terms)
      - If position > L: use the **last alphabetic letter** of the word
        (If a word somehow contains no letters after filtering, skip it and advance to the next word)
  
   c. **Convert letter to number:**
      - A=1, B=2, C=3 ... Z=26
      - Ignore case; non-letters cannot occur here by construction
  
   d. **Apply the per-die formula:**
      - raw = (letter_value × 5) + k + 13
      - **Always show the floor division and remainder explicitly**, e.g.:
        "raw 87 ÷ 6 = 14 remainder 3 → roll 4"
      - roll = (raw % die_size) + 1
      - For d6: die_size = 6
      - For d20: die_size = 20
      - For other dice (d8, d10, d12): use appropriate die_size

3. **Continue** until the full dice pool is generated

**FALLBACK METHOD (Character-Level):**
If the word-cycling sequence is exhausted before the pool is complete (rare with typical messages):
- Use **every alphabetic character** in the seed message sequentially
- Ignore case, ignore non-letters
- Apply the same per-die formula
- This guarantees unlimited entropy for any pool size

**SHADOWRUN 5E SPECIFIC (d6):**
- Hits: results of 5 or 6
- Glitch: hits = 0 AND number of 1s ≥ ceil(pool_size / 2)
- Critical Glitch: glitch condition met AND hits = 0
- Expected hits for N d6: approximately N/3 (e.g. ~4–5 for 14d6). Significant deviation from this range is possible but should be verifiable from the seed.
- For every 6 rolled, the "k" index should continue to increment using the next available letter in the sequence. This prevents the "Explosion" from being identical to the original roll.

**VERIFICATION PROCEDURE (For the Player):**
The player may independently verify any roll by:
1. Locating the seed message in chat history
2. For each die to verify:
   - Identify the source word and letter position per the cycle rule (remembering player salt if used)
   - Convert that letter to its number (A=1...Z=26)
   - Compute raw = (letter_value × 5) + k + 13
   - Check the floor division and remainder (e.g. "raw 87 ÷ 6 = 14 remainder 3")
   - roll = remainder + 1
3. Confirming the result matches the GM's narrated outcome
*Full verification of every die is unnecessary; spot-checking 2-3 dice per session establishes trust.*

**RATIONALE:**
This method transforms the LLM's deterministic architecture into a cryptographic commitment scheme. Outcomes are a function of shared conversation history—unpredictable when written, immutable afterward, and publicly verifiable. The player never needs to trust the GM; they need only verify.
Cycling through first, second, and third letters eliminates the statistical bias inherent in English first-letter frequencies. The simple ×5 multiplier and linear offset ensure good distribution and per-die independence while being extremely reliable even on basic models. Player salt adds agency and additional entropy at zero cost to verifiability.
The GM (LLM) performs all calculations instantly; the player verifies at their leisure.
The world does not run on trust. It runs on proof.

### ARCHIVED / ADVANCED VARIANTS (For stronger models or human GMs)

**Original Universal Formula (v2.3 – Mathematically Optimal)**
roll = ((letter_value × 7) + (k × 3) + 11) % die_size + 1
- Coprime multiplier (7) gives excellent cycle coverage.
- k × 3 creates stronger per-die diffusion than linear +k.
- Use this when the LLM reliably handles small multiplications.

**d6-Optimized Variant (v2.3 – Coprime-tuned for large Shadowrun pools)**
roll = ((letter_value × 13) + (k × 5) + 17) % 6 + 1
- Strongest diffusion for modulus 6.
- Substitute only for d6 rolls if the model is trustworthy with arithmetic.
- Alternate (also excellent): ((letter_value × 5) + (k × 7) + 13) % 6 + 1

These versions offer slightly flatter distributions but require more reliable multiplication and modulo handling. Use the v2.4 Simple Math Edition by default.


────────────────────────────────
### LLM IMPLEMENTATION: THE 2-BACK PROTOCOL (v3.0 – Semantic Edition)
────────────────────────────────
When the GM is an LLM that cannot generate true randomness, resolve all dice as follows.  
For Shadowrun 5E (a purely d6 system), the Semantic Cipher is the default protocol to completely bypass LLM arithmetic failure.

**SEED SELECTION:**
- Use the GM's 2nd-to-last complete message before the player's roll declaration
- If that message contains fewer than 5 words OR fewer than 8 alphabetic characters total, use the 3rd-to-last instead
- For Edge rerolls or explosions, advance to the next unused message in history (3rd-back, 4th-back, etc.)

**PLAYER SALT (Optional):**
- Before any roll, the player may add a salt word in their declaration: "Salt: [word]"
- Example: "I fire full auto. Salt: vengeance"
- The salt word is treated as the first word in the list (prepended before the seed message words)
- This adds player agency and extra entropy without compromising verifiability

**THE SEMANTIC CIPHER (Zero-Math d6 Generation)**
This protocol relies on the LLM's supreme strength—string matching and sequential pattern recognition—to generate verifiable, statistically balanced (~16.6%) d6 rolls derived directly from the prose of the world.

**THE D6 FREQUENCY BUCKETS**  
(Convert all extracted letters to UPPERCASE before matching)
- Value 1: E, C
- Value 2: T, I
- Value 3: A, O
- Value 4: N, S, M, B, V
- Value 5: H, R, L
- Value 6: D, U, W, F, G, Y, P, K, X, J, Q, Z

**DICE GENERATION RULES:**
1. Extract the List: Extract words sequentially from the seed (plus optional Salt). Ignore punctuation/numbers.  
   Fallback: If a word contains no alphabetic letters after filtering, skip it and move to the next word.
2. Target Position (1-2-3 repeating):  
   Die 1: Word 1, 1st letter  
   Die 2: Word 2, 2nd letter  
   Die 3: Word 3, 3rd letter  
   Die 4: Word 4, 1st letter (repeat cycle)  
   Wrap Rule: If a word is too short for the required position, wrap around its letters continuously. (e.g., Word "ON", needing Position 3 → wraps to 'O' (position 1 again) → Bucket 3)
3. Base Lookup: Match the target letter against the D6 Frequency Buckets to get the Base Value.
4. The Diffusion Cycle (0-1-2 repeating): Apply a repeating offset to the sequence to prevent static mapping:  
   Die 1: Base Value + 0  
   Die 2: Base Value + 1  
   Die 3: Base Value + 2  
   Die 4: Base Value + 0 (repeat cycle)  
   Subtract 6 Rule: If the final sum > 6, subtract 6. (e.g., Base Value 6 + Diffusion 2 = 8 → Roll 2)

**VERIFICATION OUTPUT FORMAT:**  
The GM must output the translation sequence to prove the roll:  
(Example for 3d6)  
D1 (pos 1, shift +0): Word [Shadow] -> 'S' -> Bucket 4. (4+0) = Roll 4  
D2 (pos 2, shift +1): Word [runs] -> 'U' -> Bucket 6. (6+1 = 7). 7-6 = Roll 1  
D3 (pos 3, shift +2): Word [in] -> Wrap pos 3 -> 'I' -> Bucket 2. (2+2) = Roll 4

**SHADOWRUN 5E SPECIFIC (d6):**
- Hits: results of 5 or 6
- Glitch: hits = 0 AND number of 1s ≥ ceil(pool_size / 2)
- Critical Glitch: glitch condition met AND hits = 0

**RATIONALE:**
This method transforms the LLM's deterministic architecture into a cryptographic commitment scheme. Outcomes are a function of shared conversation history—unpredictable when written, immutable afterward, and publicly verifiable. The world does not run on trust. It runs on proof.

**ARCHIVED VARIANTS (For Universal/Polyhedral Systems)**
If a game system uses d20s, d10s, or the LLM can reliably execute logic arithmetic over huge pools, use the mathematical modulo protocols instead of Semantic Buckets.
- Base Formula: roll = ((letter_value × 5) + k + 13) % die_size + 1 (where k starts at 0 and increments).
- Optimal Coprime Formula: roll = ((letter_value × 7) + (k × 3) + 11) % die_size + 1

EXCEPTION FOR HIDDEN ROLLS:
For purely hidden world processes (distant faction decisions, random encounter checks, etc.), 
the GM may roll privately using the same protocol, but the seed message must be noted 
in GM notes and made available to the player after the session if requested.

THE EDGE OVERRIDE:
If the Player uses an Edge point to reroll failures, the GM instantly slides 
one message further back in history (to the 3rd-back message, then 4th-back) 
to extract a fresh pool of linguistic dice without interrupting the flow of combat.

